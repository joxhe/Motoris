generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Part {
  id       String  @id @default(cuid())
  name     String
  code     String  @unique
  stock    Int     @default(0)
  price    Float
  supplier String?
}
